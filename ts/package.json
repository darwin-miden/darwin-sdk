{
  "name": "@darwin-miden/sdk",
  "version": "0.1.0",
  "description": "Darwin Protocol TypeScript SDK (wraps the Miden Web SDK).",
  "license": "MIT",
  "repository": "github:darwin-miden/darwin-sdk",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
